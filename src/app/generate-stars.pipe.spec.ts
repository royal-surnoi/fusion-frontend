import { GenerateStarsPipe } from './generate-stars.pipe';

describe('GenerateStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateStarsPipe();
    expect(pipe).toBeTruthy();
  });
});

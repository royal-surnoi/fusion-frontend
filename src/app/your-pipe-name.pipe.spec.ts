import { YourPipeNamePipe } from './your-pipe-name.pipe';

describe('YourPipeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new YourPipeNamePipe();
    expect(pipe).toBeTruthy();
  });
});

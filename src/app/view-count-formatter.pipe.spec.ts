import { ViewCountFormatterPipe } from './view-count-formatter.pipe';

describe('ViewCountFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewCountFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
